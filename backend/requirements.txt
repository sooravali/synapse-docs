# FastAPI Backend Dependencies - Size-Optimized for Docker (CPU-only)
# Reduced from ~12GB to ~3-4GB while maintaining full functionality

# Core FastAPI stack (essential)
fastapi==0.116.1
uvicorn[standard]==0.35.0
gunicorn==21.2.0
sqlmodel==0.0.24
pydantic==2.11.7
pydantic-settings==2.10.1
python-multipart==0.0.20
aiofiles==23.2.0

# HTTP client for API calls (essential)
httpx==0.28.1

# Azure TTS (Adobe Hackathon requirement)
azure-cognitiveservices-speech==1.34.0

# Google Gemini (Adobe Hackathon requirement)
google-generativeai==0.8.5

# ML Core Stack - CPU optimized (essential for Challenge 1A & 1B)
# torch and torchvision installed separately in Dockerfile with CPU-only index
sentence-transformers==5.1.0
faiss-cpu==1.11.0.post1
numpy==2.3.2
scikit-learn==1.7.1

# PDF Processing (Challenge 1A)
pdfminer.six==20250506

# Optional ML enhancements (graceful degradation when missing)
# sklearn-crfsuite  # Optional - improves heading detection but has fallback
# lingua-py  # Optional - improves language detection but has fallback
